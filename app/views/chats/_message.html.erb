<div class="mb-4 <%= message.role == 'user' ? 'text-right' : 'text-left' %>">
  <div class="<%= message.role == 'user' ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-800' %> inline-block rounded-lg px-4 py-2 max-w-[80%]">
    <p class="font-semibold text-sm mb-1"><%= message.user.email_address %> (<%= message.role.capitalize %>)</p>
    <p class="text-sm"><%= message.content %></p>
  </div>
</div>
